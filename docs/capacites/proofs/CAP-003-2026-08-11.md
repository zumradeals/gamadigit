# Preuves CAP-003 — PROFIL DE CAPACITÉS — 2026-08-11 / 2026-08-12

> **État final : VALIDÉ PROD.** Parcours principal validé par l'utilisateur en production le 2026-08-12.

## Référentiel

CAP-003 décrit une personne par ce qu'elle sait faire, souhaite apprendre et cherche à accomplir. Le profil est une source structurée d'orientation, pas une note de la personne.

## Contradiction corrigée

Avant CAP-003, `zumra_member_profiles` ne pouvait exister qu'avec `zumra_memberships`. Le profil DG était donc techniquement prisonnier de l'adhésion ZUMRA.

CAP-003 introduit `public.dg_person_profiles`, attaché directement à la référence d'identité canonique GAMAD Core et sans FK vers ZUMRA.

## Preuve Supabase

Migration : `supabase/migrations/20260811234000_dg_person_profiles.sql`.

Migration appliquée avec succès au projet `gamadigit` (`sgvzkvxefsazvsvvqtsj`). Contrôles live :

- table `public.dg_person_profiles` présente ;
- RLS activée ;
- `core_identity_reference` = clé primaire ;
- aucune FK vers `zumra_memberships` ;
- profil historique ZUMRA repris au moment de la migration ;
- aucune suppression de la table legacy ZUMRA.

## Implémentation

- `src/lib/profile/capability-profile.ts` — contrat profil CAP-003 ;
- `src/app/api/genesis/profile/route.ts` — lecture/écriture indépendante ;
- `src/app/(public)/espace/profil/page.tsx` — page Mon profil ;
- `src/components/profile/capability-profile-form.tsx` — éditeur ;
- `src/components/genesis/account-space.tsx` — charge le profil DG séparément de ZUMRA ;
- `src/components/superapp/home/member-home.tsx` — affiche/crée/gère le profil DG ;
- `/api/zumra/me` — pont uniquement pour un membre ZUMRA réel ;
- `/api/zumra/enroll` — synchronisation des champs partagés sans écraser les intentions libres DG.

## Invariants sécurité / données

- l'identité de la ligne vient uniquement de `session.entity` ;
- le client ne choisit jamais `core_identity_reference` ;
- GET et PATCH vérifient la session courante auprès de GAMAD Core via `/sessions/current` ;
- le cookie portail n'est renouvelé qu'à l'échéance attestée Core ;
- PATCH est protégé same-origin ;
- champs historiques non affichés par l'éditeur courant sont préservés ;
- profil DG ≠ adhésion ZUMRA ;
- intentions libres DG ≠ intentions contrôlées d'adhésion ZUMRA ;
- aucun score global / pourcentage de valeur de la personne n'est affiché.

## Tests automatisés production

Commit production : `59009d5450c7e00ff7cf7583d8e1e530103a6158`.

Déploiement Vercel production : `dpl_jS8o3KuFG7WyvEMHWBiz3KXYDxnu` — **READY**.

Logs de build production :

- **27 tests / 27 pass / 0 fail** ;
- CAP-001 et CAP-002 restent verts ;
- 9 tests CAP-003 ;
- compilation Next.js réussie ;
- lint/types verts ;
- génération statique **102/102** ;
- déploiement complété avec succès.

Les tests CAP-003 couvrent : profil sans dépendance ZUMRA, absence de compétence comme signal valide, limites de listes, stockage sans FK ZUMRA, liaison à une session Core live, conservation des champs non édités, séparation DG/ZUMRA, préservation des intentions libres et absence de scoring.

## Validation utilisateur production

**2026-08-12 00:22 UTC** — retour utilisateur après usage réel sur le parcours profil :

> **« profil enregistré et visible »**

Cette validation couvre le parcours principal attendu : saisie/enregistrement puis restitution visible du profil dans l'expérience DG Afrique.

Le découplage avec ZUMRA est assuré séparément par le schéma, le code et les tests : l'API profil n'a aucune création d'adhésion ZUMRA et la table canonique n'a aucune FK vers `zumra_memberships`.

## Décision

Les critères du gate sont satisfaits : parcours réel, invariants sécurité/données critiques, tests automatisés utiles et production verte.

**CAP-003 — PROFIL DE CAPACITÉS → VALIDÉ PROD.**

**CAP-004 — COMPÉTENCES peut être ouvert en EN SPEC.**
